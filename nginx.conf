server {
    listen 80;
    server_name _;

    location / {
        # Add header with the pod/container IP
        add_header server_ip $server_addr;

        # Return the IP in the response body
        default_type text/plain;
        return 200 "$server_addr\n";
    }
}
